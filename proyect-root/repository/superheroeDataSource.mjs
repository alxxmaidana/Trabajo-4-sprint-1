export default class SuperHeroeDataSource {
    // Método abstracto para obrener todos los superheroes
    obtenerTodos() {
        throw new Error("Este método debe ser implementado por la subclase");
    }
}
